---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: debian
    image: minimum2scp/systemd-stretch
    privileged: true
    command: /sbin/init
    published_ports:
      - "80"
      - "443"
      - "4040"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
...
